name: QA Background Lock

on:
  workflow_dispatch:
  push:
    branches: [ qa-background-lock ]
    paths:
      - 'app/**'
      - '.github/workflows/qa-background-lock.yml'

jobs:
  emulator-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run Android emulator background-lock test
        uses: ReactiveCircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          disable-animations: true
          script: |
            gradle --no-daemon assembleDebug assembleDebugAndroidTest --stacktrace
            adb install -r app/build/outputs/apk/debug/app-debug.apk
            adb shell locksettings set-pin 1234
            adb shell dpm set-active-admin --user 0 com.timerlock.secure/.AdminReceiver
            adb shell dpm list active-admins || true
            adb shell am instrument -w com.timerlock.secure.test/androidx.test.runner.AndroidJUnitRunner
